<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- <h1>heloooooooooooooooooooooooooooooooooo <%= 1234567890*1234567890 %></h1> -->
     <div class ="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input  class ="block w-full px-5 py-2 m-2 rounded-md bg-zinc-800" type="text" placeholder="Text" name="title">
                <textarea class ="block w-full px-5 py-2 m-2 rounded-md resize-none bg-zinc-800" type="text" placeholder="Write" name="details"></textarea>
                <input class =" block px-3 py-1 m-2 rounded-md bg-blue-500" type="Submit" value="Create Task">
            </form>
        </div>
        <div class="task p-10 flex flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %> 
                    <div class="task min-w-72 px-3 py-4 m-2 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl"><%= val %></h1>
                        <div class="flex w-full justify-between item-center mt-3">
                            <a class="text-blue-300" href="/file/<%= val %>">Read more..</a>
                            <a class="text-zinc-600" href="/edit/<%= val %>">edit filename</a>
                        </div>
                        </div>
                       
                    <% }) %>
            <% } else { %>
                <h3 class="text-zinc-600">no tasks yet.</h3>
                <% } %>            
            
            
        </div>
     </div>

    <script src="/javascript/script.js"></script>
</body>
</html>